package com.example.minimezun

import android.widget.ImageView
import android.widget.TextView

class PoolModel(pp: ByteArray, poolImg: ByteArray, poolText: String) {

    val pp: ByteArray = pp
    val poolImg: ByteArray = poolImg
    val poolText: String = poolText
}